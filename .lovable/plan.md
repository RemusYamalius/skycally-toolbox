Replace the placeholder content inside `src/routes/blog.compress-pdf-online-free.tsx` with the exact article body provided by the user.

**Changes:**
- In `src/routes/blog.compress-pdf-online-free.tsx`, remove the `{/* ARTICLE CONTENT GOES HERE */}` comment and `<p>Article content coming soon.</p>` placeholder.
- Insert the full MDX content exactly as provided, keeping all headings, lists, bold text, links, and the FAQ section intact.
- No other files are modified.

**Technical details:**
- The article uses standard Markdown/JSX elements (`h2`, `ol`, `ul`, `strong`, `a` links) that are already supported by the `prose prose-invert` wrapper in `BlogPostLayout`.
- No new dependencies or routes needed.